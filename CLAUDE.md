# 工作管理助手

半導體濕製程機台維護用的 PWA。手機隨手記錄,離線可用,雲端同步,AI 產生日報。

## 檔案結構

```
index.html              整包 app(HTML + CSS + JS,無建置步驟)
manifest.webmanifest    PWA 安裝資訊
sw.js                   Service Worker,只快取 app 外殼,不碰資料
icons/                  安裝圖示(192 / 512 / maskable)
serve.js                本機測試伺服器,node serve.js → localhost:8080
workmate-v6.html        前一版,留著對照用,不再維護
```

沒有建置流程也不打算有。改完存檔重整就是新版,這是它能一直被維護下去的前提。

**測 Service Worker 一定要用 `serve.js`。** `file://` 底下註冊不了 SW,
程式會靜靜跳過(這是刻意的,不是壞掉),但你也就測不到離線。

## 使用者與情境

維護管理人員,負責 ECU 與 EPG 兩個站別。工作環境是濕式製程區(槽液、天車、PVC 管路)。
使用情境是**站在機台旁邊拿手機記錄**,可能戴手套、環境光很亮、部分區域收訊不穩。

這決定了幾件事:操作要大、要少、要快,而且**不能等網路**。

## 核心設計原則

### 1. 全覽優先

使用者原本用 ColorNote,捨不得換掉的理由是「打開就看到全部」。
任何讓他需要點兩下才看得到清單的設計都是退步。開啟即清單,不要首頁、不要儀表板。

### 2. 離線優先(local-first)

本地是主資料,雲端是同步副本,不是反過來。

- 所有寫入先進本地,畫面立刻更新,**不等伺服器回應**
- 有網路時背景推送,使用者不需要按任何同步按鈕
- 連線狀態不影響操作手感
- 本地讀寫**永遠要有回應**。IndexedDB 交易失敗時降級記憶體,不可以讓 Promise 懸在那裡

同步狀態要顯示給使用者看(「未同步 3 筆」),因為 AI 日報只讀得到已同步的資料,
他需要知道有東西還沒上去。

### 3. 三層分離,不可混用

```
Store  →  本地儲存(IndexedDB,不支援時降級記憶體)
Data   →  資料模型(加 dirty / deleted 標記)
Sync   →  推送到後端
```

介面只呼叫 Data,**永遠不直接碰 Store 或後端**。
換後端時只改 Sync.push(),上面兩層一行都不用動。

### 4. 前端絕對不放金鑰

儲存庫是公開的。Google Sheet 網址、Apps Script 部署 ID、Claude API key
一律放後端,前端只呼叫端點。

## 資料結構:四種形態

這四類的資料行為**根本不同**,不可以共用同一種卡片或同一套欄位。

### 待辦類 — 類別由使用者自己開(預設:工作、請購、領料)

只有兩個狀態:**未處理 → 已完成**。關心「還沒結的有哪些、拖多久了」。

原本有「處理中」,拿掉了。理由是它沒有帶來對應的價值:
按下去只是心理上的「我看到了」,清單上仍然是一筆未結的東西,
卻讓每次操作多一個要判斷的選項。狀態少一個,展開後就只剩一顆大大的「完成」,
戴手套也按得準。

**代價要記住:** 少了處理中,一筆待辦從建立到完成之間 `updated` 完全不會動,
「停滯 N 天」實際上等於「開了 N 天」,而且使用者沒有辦法表達「這件事我今天有追過」。
日報想分辨「自己拖」和「等料等廠商」就沒有素材了。
真的需要的話,補一顆「今天有追」——只推一筆履歷、把 `updated` 往前帶,但不算一個狀態。

- **新的排最上面**,依建立日由新到舊
- 停滯天數仍然用 `updated` 算,不是 `created`
- 完成後顯示「建立 → 完成 · 歷時 N 天」
- 已完成清單按月份分組,由新到舊

排序曾經是反過來的——停滯最久的排最上面。改掉的理由是:
那樣一來**剛記下的東西會直接掉到清單最底下**,記完抬頭一看沒有,
會懷疑自己是不是沒存到。在機台旁邊快速記錄的場合,這個懷疑的代價太高。

**代價一樣要記住:** 拖很久的那筆不再自動浮到眼前,只剩卡片右邊那個紅色的
「停滯 N 天」和標題列的「未結 12 · 停滯 3」在提醒。
真的漏掉的話,那是日報該補的洞,不該靠排序硬扛。

### 分頁:一類一頁,已完成自成一頁

類別那一排只放類別,沒有「全部」。
各類東西的性質差太多——要修的、要買的、要領的——混在一起看沒有意義,
使用者每次進來心裡想的都是其中一類。

**已完成不在類別那一排,它站在標題旁邊。**
類別是「看哪一種東西」,已完成是「看做完的還是沒做完的」——
這是兩個不同維度,擺在同一排會讓人以為它是第四個類別。
擺在標題旁邊,層級才對:它切換的是整個畫面在看什麼。

它是**切換,不是分頁**:

- 平常是一個淺框,不搶戲。未結的才是主角,這顆只是通往 archive 的門
- 按下去進到已完成一覽(跨所有類別、按月份分組),**類別那一排整排收掉**——
  在已完成裡選類別要用篩選列,那排 chip 在這裡沒有意義
- **再按一下回到剛才那一類**,不是回到第一類。
  看完 archive 想回去繼續做事,不該還要自己找回原本在看哪一類

早期版本把已完成收在各類別清單的下面、預設收合,那個設計是錯的:
回頭查「上個月到底買了什麼」是一件獨立的事,不該先選一個類別才做得到。
中間有一版把它做成類別那排的第四顆 chip,層級也是錯的。

### 待辦的類別是使用者資料

工作 / 請購 / 領料 只是**預設值**,不是寫死的。使用者可以自己增刪。
紀錄類和專案類的類別仍然固定——那兩類的欄位和行為是綁在類別上的
(掛籃飛靶要編號、專案要算停滯),開放自訂只會做出壞掉的組合。

類別存在 IndexedDB 的 `meta` 表(v2 加的),不是 localStorage——
它是要跟著同步走的資料,不是這台裝置的偏好設定。

**顏色跟著類別存,不是照順序算的。**
照順序算的話,刪掉中間一個,後面每一類的卡片顏色都會跟著換一輪,
使用者會覺得整個 app 突然變了個樣。

**刪類別一定要先問東西搬去哪。**
底下還有東西就直接刪的話,那些卡片不屬於任何分頁,在畫面上等於消失了,
但它其實還好好躺在資料庫裡——這比真的刪掉更糟,因為使用者不會知道要去找。
所以:空的才直接刪;有東西就先指定搬到哪一類,搬完再刪。連墓碑一起搬,
否則哪天復原回來,又冒出一個誰都不認得的類別。

搬類別算**內容變更**,動 `edited` 不動 `updated`。
換個類別而已,停滯天數不該歸零。

至少要留一個類別。「已完成」和「全部」是分頁名,不能拿來當類別名。

### 紀錄類 — 保養紀錄、更換備品紀錄、掛籃紀錄、飛靶紀錄

流水帳,沒有「完成」這回事。記下的當下就結束了。
關心「這台上次做是什麼時候、最近是不是太頻繁」。

掛籃和飛靶是**有編號的個體**,會在狀態間循環:

```
備用 → 上線 → 換下(定期/異常)→ 保養 → 備用
```

所以它們有獨立的 `code`(編號)和 `event`(事件)欄位。
編號必須是獨立欄位而不是寫在內容裡,否則 AI 算不出「B-13 這季異常換下三次」這種頻率異常。

同理,**編號存進去前一律轉大寫,而且不准留空**。
`b-13` 和 `B-13` 若被當成兩個個體,頻率就永遠算不對;沒編號的那筆則是直接消失在統計外。
這兩件事在 UI 層就擋掉,不要留給日報去猜。

保養項目**部分有固定週期、部分沒有**。有週期的才進日報提醒。

### 專案類 — 化銅線漏液改善追蹤、鎳鈀金線漏液改善追蹤

長期容器,底下掛一則則有日期的進度更新。
關心「多久沒動了」。超過 14 天沒更新要示警——專案最容易放著放著就忘了。

### 調查類 — 一次量測、一批數字

一筆調查底下有**很多筆量測**,這是它跟紀錄類最大的差別:
紀錄是一筆一件事,調查是一筆一整張表。使用者原本在 ColorNote 上就是這樣記的:

```
Ecu抽風統計 共12口        馬座承屑損壞統計        26_28 打藥功率
  #3   3.7m/s              #1  2                   220+130
  #8   4.9                 #3  1                   換後 158
  #13  1.0                 ...                     220+80
  ...                      共26個                   換後 208
```

關心的是「**這次跟上次差多少**」——哪一口風速掉了、哪一區又多壞了幾個。

**輸入維持自由文字,存的時候才解析。**
這是這一類設計的核心。要求使用者填表格的話,站在機台旁邊就沒人會用;
但完全不解析的話,就只是一坨文字,算不出總數也比不出差異,日報永遠講不出
「#28 這次比上個月掉了 10%」。

所以:一行一筆,`位置 數值`,第一個詞當位置、後面第一個數字當數值、剩下的當單位。
**拆不出來的行原樣留著當備註,絕對不吃掉。**
使用者手寫的「共26個」「220+130」都會留著,而總數由程式自己算——
他手算的那個數字本來就常常忘記更新。

- 整數 → 當成「在數東西」,顯示**合計**
- 有小數 → 當成「在量東西」,顯示**平均**

這個判斷刻意自動化,不要多問一個「這是計數還是量測」——多一個問題就少一個人用。

**同一個標題就是同一份調查。** 展開時自動跟上一次同名的比對,
變大 ▲、變小 ▼、沒變 —、這次才有的標「新」。
**上次有、這次沒量到的也要列出來**,否則它會靜悄悄地消失,
而「這次沒量到」跟「這次量到 0」是完全不同的兩件事。

### ECD:有明確到期日的才填

使用者本來就在內文裡手寫「ECD 9/5」「壓九月完成」。獨立成欄位之後才算得出逾期。

**卡片右上角只放一個時間訊息,有 ECD 就講 ECD。**
有明確到期日的時候,「還剩三天」比「停滯十二天」有用得多;沒有 ECD 才回頭講停滯。
兩個都顯示的話,那一行會擠,而且使用者得自己判斷該看哪個。

- 逾期 → 紅字「逾期 N 天」
- 今天 → 紅字「今天到期」
- 七天內 → 琥珀色「剩 N 天」
- 更遠 → 灰字「9/20 到期」

標題列的計數同理:**有逾期就先講逾期**,沒有才講停滯。

### 手動排序

長按卡片可以拖曳調整順序。排序值放在 `order`,**預設是 `-created`**——
所以沒手動排過的時候,順序就等於「新的在最上面」,舊資料不會因為多了這個欄位而重排。

**一次拖曳只寫一筆。** 新的 `order` 取前後兩張卡的中間值(小數)。
整批重新編號的話,拖一次就要寫幾百筆、全部變成待上傳,在離線同步的前提下太貴。
只有當中間值擠不下了(浮點數精度用完)才重新編號一次。

**排序既不是狀態變更也不是內容變更**,所以 `updated` 和 `edited` 都不動。
把某一筆拉到最上面,它的停滯天數不該歸零——那是兩件事。

### 三個時間欄位,不可以混用

| 欄位 | 意思 | 誰會動它 |
|---|---|---|
| `created` | 建立時間 | 只有建立 |
| `updated` | **狀態**最後有進展的時間 | 只有改狀態 |
| `edited` | 內容最後被修改的時間 | 只有編輯 |

**編輯內容絕對不能動到 `updated`。**
停滯天數和排序都靠 `updated`,改一個錯字就把計時歸零的話,
拖最久的那筆會直接從清單頂端消失——那正好是最需要被看見的一筆。

## 站別與機台

機台清單**跟著站別連動**。切換站別時,若原機台在新站別不存在要自動清空,
不能產生 ECU 配 Plasma 這種不可能的組合。

```
ECU: 主線、敘豐A part、敘豐B part、EFEM投板、EFEM收板、IPQC

EPG: 主線、敘豐A part、敘豐B part、敘豐前處理、敘豐後處理、
     IPQC、EFEM投板、EFEM收板、Plasma、Xray
```

## 機台配色

十台機台十個顏色記不住,所以配色**照機群走**:

| 機群 | 機台 | 色系 |
|---|---|---|
| 主線 | 主線 | 綠 |
| 敘豐 | A part / B part / 前處理 / 後處理 | 藍 → 紫藍(同色系,靠深淺分) |
| EFEM | 投板 / 收板 | 紫 / 洋紅 |
| 其他 | IPQC / Plasma / Xray | 橘 / 紅 / 橄欖 |

記不住「敘豐B part 是哪個藍」沒關係,記得住「藍的那群是敘豐」就夠用了。

**同名機台在兩個站別用同一個顏色。**
站別本來就靠粗體字分,不需要再用顏色分一次;
反過來說,這樣「所有主線的事情」在兩個站別之間也是同一個顏色,掃得出來。

沒指定機台的(請購常常沒有)用中性灰,而且不畫圓點——
沒有機台跟「機台是灰色那台」必須看得出差別。

## 介面約定

- **新增用置中彈出視窗**,不要從底部滑出的 sheet
- 新增表單以**內容為主體**:大字輸入框在最上面、自動聚焦、無邊框無標籤。
  站別機台等選項縮小放底下,是配角
- 新增時**延續使用者當前所在的分頁**,少點一次;站別也延續上一次選的
- 改狀態**不跳頁**,在原地展開動作按鈕,不讓使用者失去清單位置
- **卡片左側色條標示機台,不是類別。**
  類別各自一頁之後,同一頁裡每張卡的類別都一樣,色條擺類別等於沒帶資訊。
  類別改由旁邊那顆有顏色的標籤負責——標籤上有字,不會認錯
- 站別用粗體、機台接在後面(`ECU · ● 敘豐A part`),站別是第一眼要分辨的層級。
  機台名前面那顆點跟色條同色,兩者一眼對得起來
- **機台名本身不上色**,維持深色。廠區光線很亮,彩色小字是最先看不清楚的東西。
  顏色只放在色塊上(色條、圓點),文字永遠保持高對比
- 新增視窗裡的機台選項也帶著同一顆色點,選的當下就建立起顏色與機台的連結
- **編輯沿用同一個視窗**,只是預先填好。不要為編輯另做一套表單
- **篩選列用原生 `<select>`,不要自己刻。**
  Android 會跳出系統選單,選項又大又好按,戴手套也點得到,
  而且不管幾個選項都只吃畫面一列的高度。自己刻一排 chip 會多吃兩三列
- 篩選**跨分頁保留**。在 ECU 主線看完工作,切到請購時通常還想看同一台。
  有篩選在作用時要出現「清除」,否則使用者會忘記自己還在某個範圍裡,
  以為東西不見了
- **分頁上的計數要跟著篩選走**,不然數字跟眼前的清單對不起來,比沒有數字更糟
- 篩選中新增時,**站別機台沿用篩選的值**。正在看 ECU 主線,要記的多半也是那台
- 篩到空的時候,提示要說「這個範圍沒有」,不能只說「沒有」
- 展開後動作分兩排:上排是狀態(大按鈕,最常按),下排是編輯與刪除。
  四顆擠一排在戴手套時按不準
- **刪除一定要能復原**。刪除本來就只是標記墓碑,復原幾乎零成本,
  而戴手套誤觸的機率不低。刪完跳的提示要帶「復原」按鈕
- **長按 450ms 才進入拖曳,並且震一下。**
  太短會跟捲動搶,太長使用者會以為沒反應。這 450ms 內手指移動超過 10px
  就當成是在捲動,直接放棄
- 拖曳時**只畫一條落點線,不去推擠其他卡片**。
  卡片高度不一(內容有長有短),推擠的位移一定會算歪;一條線永遠是準的
- 拖到清單上下緣要**自動捲動**。183 筆的清單若不會自動捲,
  就只能在同一個畫面裡搬,等於沒用
- 調查的明細輸入框用**等寬字**。位置那一欄對得齊,錯行一眼看得出來
- 調查沒有類別可選(只有一種),那一列直接藏起來,少一個要看的東西
- 管理類別的入口是分頁列最後那顆虛線的「＋ 類別」。
  新增和刪除放同一個視窗,每一類旁邊標著「還有幾筆」——
  那個數字就是刪之前唯一要知道的事
- 示範資料只在網址帶 `?demo` 時才灌。正式使用是空的開始

## 版面密度

手機一屏就那麼高,每一列固定佔用的高度都是從卡片身上扣的。以 780px 可視高度估算,
瘦身前上下固定佔 261px,一張卡 98px,一屏看得到 5.3 張。

拿掉的東西與理由:

- **卡片上的「建立 9/06」** — 佔一張卡的 22%,而右邊的「停滯 N 天」講的是同一件事的
  更有用版本。收進展開後才顯示,一屏多 1.5 張
- **卡片上的「未處理」** — 已完成有自己一頁之後,未結清單裡每張卡都是未處理,零資訊
- **同步列的「已同步」** — 一切正常時固定吃掉 43px 說一句廢話。改成有話要說
  (未同步 N 筆、離線中)才出現,**沒出現本身就是「都上去了」的意思**

沒有拿掉的:

- **底部分頁列** — 併成一行可以省 18px,但只多 0.25 張卡片,
  而它是最主要的導覽按鈕。用觸控目標去換四分之一張卡,不划算
  (「戴手套要按得準」那條優先)

判斷的順序是:**先找零資訊的東西,再考慮壓縮有資訊的東西。**
前者是免費的,後者一定有代價。

## 已知的坑

**改資料結構時,使用者手機裡的舊資料不會跟著變。**
讀取時必須有一層 `fix()` 補齊缺少的欄位,否則新版在舊裝置上會整個壞掉。
這個坑已經踩過一次。

`fix()` 要做的不只是補缺欄位,還要**擋掉不可能的組合**。
`family`、`cat`、`sta`、`mac`、`status` 之間互相有約束,
一筆 `family=待辦` 卻 `cat=保養紀錄` 的舊資料會讓色條算出 `kundefined`,
`status=已完成` 卻沒有 `doneAt` 則會在畫面上渲染出 NaN。這些都要在讀取時就正規化掉。

**拿掉一個狀態,不等於手機裡那些狀態就消失了。**
v6 記過的「處理中」還在使用者的 IndexedDB 裡。
`LEGACY_ST` 負責把它併回未處理——不能直接丟給「非法值就歸零」那條去處理,
那樣雖然結果一樣,但意圖不明確,下次再刪狀態的人會不知道這裡曾經有過什麼。

**不認得的類別絕對不可以默默歸到第一類。**
`fix()` 對紀錄和專案的類別做白名單比對(那兩個是固定的),
但待辦的類別是使用者開的,**別台裝置同步下來的類別本機可能還沒有**。
照舊那套「不認得就歸到 cats[0]」會把那批資料默默搬走,而且完全看不出來。
正確作法是照原樣留著,再由 `Cats.reconcile()` 把沒見過的類別補進清單。
沒有 reconcile 的話那些卡片不屬於任何分頁,在畫面上等於不存在——
這是「資料不會消失」的最後一道保險,不能省。

**改資料庫版本時,onupgradeneeded 要對全新和既有的資料庫都能跑。**
v2 加了 `meta` 表。使用者手機裡是 v1,只有 `items`;新裝置是全新的,兩個都沒有。
所以每個 store 都要各自 `if(!db.objectStoreNames.contains(...))` 再建,
不能假設前一版留下了什麼。

**Android Chrome 會把你的點擊當成「在查一個詞」。**
Touch to Search:點到可被選取的文字,Chrome 會自己跳出 Google 搜尋那個詞。
這個 app 幾乎每個字都長在一個可以點的東西上面(底部分頁、chip、卡片、動作鈕),
所以整體 `user-select:none`,只在真的要打字的 input / textarea 開回來。
順帶擋掉長按的複製選單和拖曳的藍色反白——戴手套快速點按時那些全都是誤觸。

代價是卡片內文不能長按複製了。真的需要複製料號的話再想別的辦法
(例如展開後給一顆複製鈕),但不能用「文字可選取」換,那會把誤觸帶回來。

**列與列之間的留白要能撐過「上一列被收掉」。**
看已完成時類別那排整排 `display:none`,不生成任何框,
只給下緣留白的那一列就會直接貼到再上面一列去。
上下都給,相鄰 margin 會合併,類別列在的時候也不會變成兩倍。

**同一份資料不要存兩份。**
調查的 `rows` / `notes` 是從 `detail` 解析出來的,**每次讀取都重新算**,不另外存。
兩邊都存的話遲早會不一致,而不一致的時候你不知道該信哪一個。
(要送給後端做 AI 分析的結構化資料也是從讀取結果來的,所以不會少。)

**刪除要留墓碑。**
離線刪除若只從本地移除,下次同步會被雲端當成「本地沒有的新資料」抓回來,死者復活。
必須標記 `deleted: true` 推上去。

**編號在手機端產生。**
離線時拿不到伺服器流水號。用時間戳加隨機字串。不可以用 Sheet 列號當識別,那個會變。

**推送失敗時保留 dirty 標記。**
寧可重複送一筆(後端用 id 覆蓋),也不能誤判送成功而清掉標記,那筆就永遠上不去了。

**成功路徑也會吃掉編輯。**
上面那條講的是失敗,但送出成功同樣有洞:取出待送清單 → await 網路 → 回來清 dirty,
這中間使用者可能改了同一筆。若用送出前的舊快照寫回 `{...it, dirty:false}`,
新編輯會被舊內容蓋掉,而且 dirty 被順手清掉,那次修改就永遠上不去。
作法是**送出前後比對指紋**(`updated|edited|deleted|status`),
不一致就跳過、保留 dirty,下一輪重送。這條有測試在守。

**Apps Script 不能做真正的 PWA。**
它的網頁包在 googleusercontent 的 iframe 裡,Service Worker 註冊不了。
所以前端要放 GitHub Pages,Apps Script 只當回傳 JSON 的後端。
CORS 用 `text/plain` 送出可以避開瀏覽器預檢請求。

**Service Worker 更新不要自動重載頁面。**
使用者可能正在打字。偵測到新版只跳提示,讓他自己決定什麼時候重開。
改版時記得把 `sw.js` 裡的 `CACHE` 版本號往上加,否則舊外殼會一直被吃到。

## 路線圖

- [x] 介面雛形,三大類分頁
- [x] 離線資料層與同步狀態顯示(目前 Sync 是模擬的)
- [x] 編輯既有項目、刪除可復原
- [x] 待辦簡化成兩個狀態
- [x] 站別 / 機台 / 類別篩選,已完成獨立成一覽,新的排最上面
- [x] 待辦類別開放自訂(新增 / 刪除 / 搬移),存在 IndexedDB meta 表
- [x] 機台配色,色條由類別改標機台
- [x] 已完成改成標題旁的切換鈕,進去時收起類別列
- [x] 站別允許空白(ColorNote 匯進來的資料有一半看不出站別)
- [x] ColorNote 匯入(`?import` 讀同目錄的 import.json,id 由來源決定可重跑)
- [x] 調查類:自由文字輸入、自動解析、跟上一次比對
- [x] ECD 到期日、卡片瘦身、同步列改成有事才出現
- [x] 長按拖曳手動排序
- [x] Service Worker + manifest,可安裝、離線可開啟
- [ ] 部署到 GitHub Pages,手機加到主畫面
- [ ] Google Sheet 建表 + Apps Script 後端,替換掉模擬的 Sync.push()
- [ ] 定時觸發,先做純規則日報(今日新增、停滯超過 N 天、本週保養)
- [ ] 接 Claude API,做頻率異常偵測與輕重緩急判斷
- [ ] 備品消耗速度 → 自動請購提醒

## 日報的價值在哪

日報如果只是把今天記的唸一遍就沒有意義。它要講的是**使用者自己沒注意到的事**:

- 待辦拖太久了(想區分「自己拖」和「等料等廠商」的話,得先有「今天有追」那顆按鈕,見上面待辦類那節)
- 某支掛籃異常換下頻率遠高於同型其他支,可能該報廢而不是再送保養
- 保養超過週期沒做
- 專案停滯
- 備用件依照最近消耗速度大約幾天會用完
- 調查的數字往哪個方向走(某一口抽風連續三次下降、某一區損壞數量一直加)
